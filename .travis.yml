language: python

python:
  - 3.5

sudo: false

install:
  - pip install tox
  - pip install python-coveralls

addons:
  apt:
    sources:
      - deadsnakes
    packages:
      - python3.5

script: tox

after_success:
  - coveralls

cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/.tox
