language: python

install:
  - pip install tox
  - pip install python-coveralls

addons:
  apt:
    sources:
      - deadsnakes
    packages:
      - python3.5

script: tox

after_success:
  - coveralls

cache:
  - $HOME/.cache/pip
  - $HOME/.tox
